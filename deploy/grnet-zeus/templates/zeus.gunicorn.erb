CONFIG = {
    'mode': 'wsgi',
    'environment': {
      'DJANGO_SETTINGS_MODULE': 'settings',
    },
    'user': 'www-data',
    'group': 'celery',
    'working_dir': '/srv/zeus_app',
    'args': (
        '--bind=[::]:8080',
        '--log-file=/var/log/zeus-gunicorn.log',
        '--workers=8',
        '--worker-connections=512',
        '--umask=0077',
        '--timeout=43200',
        'wsgi:application'
    ),
}
